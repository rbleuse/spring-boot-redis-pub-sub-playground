1. Model routing by task type. Haiku for PR descriptions and simple lookups. Sonnet for architecture reviews. Never auto-escalate.
2. Before opening any file, confirm it contains what you need with grep first.
3. After 10 tool calls without visible progress, stop and explain the blocker.
4. Never read entire directories. Use find or grep to locate files first.
5. Never run: git push, npm publish, database migrations without explicit user confirmation.
